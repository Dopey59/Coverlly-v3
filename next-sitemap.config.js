/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://www.coverlly.fr', // URL de votre site
    generateRobotsTxt: true, // Générer un fichier robots.txt
  };
  
  export default config;
  